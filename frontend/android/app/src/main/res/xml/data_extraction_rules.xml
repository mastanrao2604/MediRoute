<?xml version="1.0" encoding="utf-8"?>
<!--
    Data extraction rules for Android 12+ (cloud backup & D2D transfer).
    Disables all backup/restore to prevent auth tokens stored in WebView
    localStorage or SharedPreferences from being extracted.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="sharedpref" path="." />
        <exclude domain="database" path="." />
        <exclude domain="file" path="." />
        <exclude domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="sharedpref" path="." />
        <exclude domain="database" path="." />
        <exclude domain="file" path="." />
        <exclude domain="external" path="." />
    </device-transfer>
</data-extraction-rules>
