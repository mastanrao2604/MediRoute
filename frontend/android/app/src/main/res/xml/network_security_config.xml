<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Production: HTTPS only, trust system CAs -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <!-- Allow cleartext to local/dev addresses -->
    <!-- 10.0.2.2     = Android emulator host-loopback alias                  -->
    <!-- 127.0.0.1    = loopback                                               -->
    <!-- 10.116.142.143 = dev machine WiFi IP (update if network changes)      -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        <domain includeSubdomains="true">10.116.142.143</domain>
    </domain-config>
    <!-- Trust user-installed CAs in debug builds (e.g. Charles Proxy) -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
